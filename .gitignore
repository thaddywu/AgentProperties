# Virtualenv
.venv/

# Python bytecode
__pycache__/
*.pyc

# Third-party benchmark corpora, cloned/downloaded, ~40G. Not ours to vendor.
# Provenance and per-file pointers live on the archive-tsm-fs-spec branch
# (survey/dataset.md, survey/pointer.md).
datasets/

# Bulk extraction from the ToolBench corpus, regenerable from datasets/.
examples/toolbench/

# Leftover read-only working-tree residue from the pre-rewrite experiments; the
# real copies are committed on archive-tsm-fs-spec. Delete this stanza once the
# directory is removed from disk.
applications/opsdesk/results/
