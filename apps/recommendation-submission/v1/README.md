# Recommendation Submission — application v1

This frozen application version is the first SafeMA case study.

```text
app-spec-v1.md   frozen Base App specification
policy-v1.md     frozen natural-language application policy
base-app/        independently runnable Base App
safema-v1/       integration with the shared repository-root SafeMA v1 runtime
```

The Base App and application documents belong to this application version.
The reusable runtime is located at [`../../../safema-v1`](../../../safema-v1/README.md).
